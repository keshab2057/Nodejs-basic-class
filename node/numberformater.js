//write a js function that puts comma in every thousand place
//eg:10000=>10,000
//1000=>1,000
//1000000=>1,000,000

const format = (N)=>{
const newFormat = givenNumber.toLocalString("en-us"); 
return newFormat;   
};
const res = format(1000);
console.log(res);
